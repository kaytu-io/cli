// ========== DO NOT EDIT THIS FILE! AUTO GENERATED!!! =========
package get

import (
	"errors"

	"github.com/spf13/cobra"

	"github.com/kaytu-io/cli-program/cmd/genv2/kaytu/get/insight"
)

var InsightCmd = &cobra.Command{
	Use: "insight",
	RunE: func(cmd *cobra.Command, args []string) error {

		if cmd.Flags().ParseErrorsWhitelist.UnknownFlags {
			return errors.New("invalid flags")
		}
		return cmd.Help()

	},
}

func init() {

	InsightCmd.AddCommand(insight.TrendCmd)

	InsightCmd.AddCommand(insight.MetadataCmd)

	InsightCmd.Flags().StringArray("connection-group", nil, "filter the result by connection group")

	InsightCmd.Flags().StringArray("connection-id", nil, "filter the result by source id")

	InsightCmd.Flags().Int64("end-time", 0, "unix seconds for the end time of the trend")

	InsightCmd.Flags().String("insight-id", "", "Insight ID")

	InsightCmd.MarkFlagRequired("insight-id")

	InsightCmd.Flags().StringArray("resource-collection", nil, "Resource collection IDs to filter by")

	InsightCmd.Flags().Int64("start-time", 0, "unix seconds for the start time of the trend")

}
